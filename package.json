{
  "scripts": {
    "build": "parcel build --no-minify --no-content-hash src/*.js src/*.html --out-dir build/"
  },
  "dependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.6.1"
  }
}
